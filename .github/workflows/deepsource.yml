name: DeepSource

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deepsource:
    runs-on: ubuntu-latest
    env:
      DEEPSOURCE_DSN: ${{ secrets.DEEPSOURCE_DSN }}

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - uses: astral-sh/setup-uv@eac588ad8def6316056a12d4907a9d4d84ff7a3b
        with:
          enable-cache: true

      - name: Require uv.lock
        run: |
          set -euo pipefail
          test -s uv.lock || (echo "uv.lock missing — run uv lock"; exit 1)

      - name: Install deps (dev + cli + server + grpc)
        run: |
          set -euo pipefail
          uv sync --extra dev --extra cli --extra server --extra grpc

      - name: Generate coverage report
        run: |
          set -euo pipefail
          ./scripts/generate_grpc_stubs.sh
          make coverage
          test -s coverage.xml

      - name: Upload coverage to DeepSource
        if: ${{ env.DEEPSOURCE_DSN != '' }}
        uses: deepsourcelabs/test-coverage-action@4284bb73a04adb39faaa6bfcc2d0e3dd137ffed7
        with:
          key: python
          coverage-file: coverage.xml
          dsn: ${{ env.DEEPSOURCE_DSN }}

      - name: DeepSource DSN not configured
        if: ${{ env.DEEPSOURCE_DSN == '' }}
        run: |
          echo "DEEPSOURCE_DSN is not set. Add it as a repository secret to enable coverage upload."
